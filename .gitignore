# Node dependencies
node_modules/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Runtime data
pids
*.pid
*.seed

# Coverage
coverage/
*.lcov

# Build artifacts / bundles
# Keep dist committed for GitHub Action (bundled with ncc)
!.gitignore
.tmp/
.temp/
!dist/

# OS / Editor
.DS_Store
*.swp
.idea/
.vscode/

# Env files
.env
.env.local
.env.*.local

# Lock files (optional if publishing a reusable action with vendored deps)
# Uncomment if you want to ignore lock files
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

# Action specific
# If later bundling with ncc outputs additional assets
*.map
